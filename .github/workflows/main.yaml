on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: okteto/actions/namespace@RRtD4FrhDkzJJqdQZkxoI097o62ChokGOdNX6qZS
      with:
        token: ${{ secrets.OKTETO_TOKEN }}
        namespace: actions-rberrelleza
        
    - uses: okteto/actions/deploy@RRtD4FrhDkzJJqdQZkxoI097o62ChokGOdNX6qZS
      with:
        namespace: actions-rberrelleza
        manifests: |
          k8s.yml